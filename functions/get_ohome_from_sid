
#--------------------------------------------------------------------------------
#
# function get_ohome_from_sid
#
#--------------------------------------------------------------------------------

function get_ohome_from_sid
    {
    if [ "${1}" != "" ]
    then
        sid=${1}
    else
        sid=${ORACLE_SID}
    fi
    grep ^${sid}: ${ORATAB} | cut -d: -f2
    }
