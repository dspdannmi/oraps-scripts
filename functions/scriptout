
#---------------------------------------------------------------------------------------
#
# function scriptout
#
#---------------------------------------------------------------------------------------

function scriptout
    {
    if [ "${scriptname}" = "" ]
    then
	:
    else
	echo "${scriptname}:    ${*}" | tee -a ${LOGFILE}
    fi
    }

